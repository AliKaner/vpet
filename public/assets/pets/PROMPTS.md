# Pet atlas

Preview: open `preview.html`. The generated last two rows are slightly offset: runtime uses a 97.5% vertical background position for horse, and clips the bottom padding for mouse (12%) and snake (3%) to prevent adjacent-row bleed. The transparent alpha channel is preserved. Browser-rendered gallery checked in headless Edge.

Generated with the built-in image_gen tool. v1 is the initial source; v2 is the background correction used by the game. 4 columns (idle, feed, affection, clean), 6 rows (cat, dog, bird, snake, mouse, horse). Each cell is 256 x 256 pixels. Runtime motion is CSS animation of individual poses, not frame-by-frame limb animation. Species-specific actions use the corresponding care pose and their own motion and message.

Use case: stylized-concept. Asset type: production pixel-art virtual pet sprite atlas, single PNG with genuinely transparent background. Make a precisely aligned 4-column by 6-row regular grid, 1024x1536 canvas, each cell 256x256. No labels, text, borders, gridlines, scenery or shadows outside characters. Every cell contains one complete centered cute animal, same scale and same baseline within each row, ample transparent padding. Crisp chunky pixel art, dark cocoa outlines, pastel cozy game palette, recognizable expressive faces, no smoothing. Rows top to bottom: orange tabby cat; golden floppy-eared dog; turquoise small bird; mint green coiled snake; lavender gray mouse with pink ears; chestnut pony with cream mane. Columns left to right: 1 neutral happy idle with open eyes; 2 feeding reaction leaning face down munching food from small bowl (bird seeds, snake tiny food pellet, mouse cheese, horse hay); 3 being petted reaction eyes closed delighted with two tiny pink hearts overhead; 4 washing reaction eyes squinting playful with blue soap bubbles around body. Keep each species visually identical between its four poses but distinctly change the pose and facial expression for each action. Exactly 24 sprites; all fully inside their own equal cells. Transparent alpha background essential.

Background correction prompt:
Edit this exact sprite atlas. Remove the entire colored blurry background and replace it with genuine transparent alpha (not a checkerboard or solid color). Preserve the exact 1024x1536 canvas, all 24 sprites in their exact positions, colors and pixel edges. Keep hearts, bowls, bubbles. No rearranging or resizing anything. Only remove background.
